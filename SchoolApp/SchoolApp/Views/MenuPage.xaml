<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SchoolApp.Views.MenuPage"
             Title="Welcome To SchoolApp"
             Padding="0,40,0,0"
             BackgroundColor="#2196F3"
             >
    <StackLayout>
        <ListView ItemsSource="{Binding MenuItems}" SeparatorVisibility="Default">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell ImageSource="" Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>  
</ContentPage>

<!--<ViewCell>
    <Grid Padding="5,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        --><!--<Image Source="" />--><!--
        <Label Grid.Column="1" Text="Courses" VerticalOptions="FillAndExpand"
                                TextColor="White"/>
    </Grid>
</ViewCell>-->